project: '${PROJECT_NAME:-apis-hub}'
timezone: '${APP_TIMEZONE:-UTC}'
debug: true
jobs:
  timeout_hours: 6
analytics:
  cache_raw_metrics: false

# Cron Scheduling Defaults
# Deprecated: The system now uses UI-defined values or a dynamic 15-minute stagger starting at 4:00 AM.
